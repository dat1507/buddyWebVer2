# GitHub repository presentation

Target: **dat1507/buddyWebVer2**.

## Prepared About fields

**Description**

```text
VGU Buddy: a React and TypeScript student companion platform in development for Vietnamese-German University, with an EN/DE public website and authentication UI.
```

**Topics**

```text
react typescript vite tailwindcss react-router tanstack-query i18next student-community buddy-program vietnamese-german-university
```

**Website**

Leave empty until a deployment of this rebuild is verified. The legacy Buddy website is a different application and should not be labeled as the live demo of this repository.

## Apply to GitHub

The connected browser was logged out during the audit, and GitHub CLI was not available on PATH. These fields have been prepared but have **not** been applied remotely.

1. Open this repository in GitHub while signed in with repository settings access.
2. Open the gear beside **About** on the Code page, paste the description and topics above, and save.
3. Commit and push the documentation and screenshot files to the repository's default branch, or merge them through a reviewed pull request. The root `README.md` must be included for the repository landing page to show it.
4. Check the rendered README: existing logo, badges, screenshot paths, collapsible previews, Mermaid diagram, and documentation links.

Suggested commit subject:

```text
docs: professionalize repository presentation and document current frontend scope
```

No repository rename, license selection, release, deployment claim, or workflow badge is required for this presentation update.

## Keep the presentation accurate

- Change a feature to implemented only after verifying the source and its relevant behavior.
- Add a live URL only after verifying it runs this rebuild.
- Add a CI badge only after a real workflow exists and the badge targets that workflow.
- Add a license badge only after the maintainer chooses and adds a project license.
- Refresh screenshots after visible UI changes and record the capture context.
- Keep the README's current architecture separate from the implementation plan's future architecture.
